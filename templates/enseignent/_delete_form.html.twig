<form method="post" action="{{ path('app_enseignent_delete', {'ide': enseignent.ide}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enseignent.ide) }}">
    <button class="btn">Delete</button>
</form>
