
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn" style="background-color: #2980B9 ; color: white; padding: 5px; border: none;
                                      border-radius: 5px; text-decoration: none; cursor: pointer;
                                      display: inline-block; width: fit-content; margin-bottom : 30px">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
</div>
