<form method="post" action="{{ path('app_formationB_delete', {'idFormation': formation.idFormation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.idFormation) }}">
    <button class="btn btn-danger">Delete</button>
</form>
