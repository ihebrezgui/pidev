{% extends 'baseBack.html.twig' %}

{% block gestion %}
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">Modifier utilisateur</div>
                    <div class="card-body">
                        {{ include('back/utilisateur/_form.html.twig', {'button_label': 'Update'}) }}
                    </div>
                </div>
                <div class="mt-3">
                    <a href="{{ path('app_utilisateur_index') }}" class="btn btn-secondary">Retour à la liste</a>
                </div>
                <div class="card mt-3">
                    <div class="card-body">
                        {{ include('back/utilisateur/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
