<form method="post" action="{{ path('app_code_promo_delete', {'idPromo': code_promo.idPromo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code_promo.idPromo) }}">
    <button class="btn">Supprimer</button>
</form>
