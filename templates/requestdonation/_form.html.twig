{{ form_start(form,  {'attr': {'novalidate': 'novalidate'}}) }}

  <div class="row">
    <div class="col-lg-6">
      {{ form_row(form.formation_souhaitee) }}
      {{ form_row(form.email) }}
      {{ form_row(form.dateLimite) }}
      {{ form_row(form.lieu_de_residence) }}
    </div>
    <div class="col-lg-6">
      {{ form_row(form.description) }}
    </div>
  </div>
  <button type="submit" class="btn btn-primary"> {{ button_label|default('Enregistrer') }}</button>
  <head>
  <style>
  .card {
    max-width: 400px; /* Adjust as needed */
    background-color: #F0F2F5;
    border-radius: 5px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    margin: 50px auto;
    padding: 20px;
  }

  .form-row {
    margin-bottom: 15px;
  }

  .form-row label {
    display: inline-block;
    width: 100px;
    text-align: right;
  }

  .form-group input[type="text"],
  .form-group input[type="email"],
  .form-group input[type="date"] {
    display: block;
    width: 100%;
    padding: 8px 10px;
    border: 1px solid #ccc;
    border-radius: 3px;
    font-size: 14px;
  }

  .btn-primary {
    background-color: #2980B9;
    border-color: #2980B9;
    color: white;
    padding: 8px 16px;
    border-radius: 50px;
    cursor: pointer;
    float: right; /* Or adjust position as needed */
  }
    </style>
  </head>

{{ form_end(form) }}

